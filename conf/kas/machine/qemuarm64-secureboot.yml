header:
  version: 11
  includes:
    - ../base.yml

repos:
  meta-arm:
    url: https://git.yoctoproject.org/meta-arm
    refspec: kirkstone-g96aad3b29aa7a5ee4df5cf617a6336e5218fa9bd
    layers:
      meta-arm:
      meta-arm-toolchain:

local_conf_header:
  qemuall: |
    UBOOT_EXTLINUX = "1"
    UBOOT_EXTLINUX_INITRD = "../avocado-initramfs"
    UBOOT_EXTLINUX_ROOT = "root=/dev/vda2"
    UBOOT_EXTLINUX_CONSOLE = ""

machine: qemuarm64-secureboot
